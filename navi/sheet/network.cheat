% network

# check what processes are listening (only for macOS)
lsof -iTCP -sTCP:LISTEN -P -n

# get Local IP Address (only for macOS)
ifconfig -a | grep "inet "

# Speed test (only for macOS)
networkQuality -v

# Get IP information
curl -s https://ipinfo.io/json | jq
